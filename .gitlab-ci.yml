stages:
  - Build

Dependencies:
  image: node:latest
  stage: Build
  script:
    - npm install
    - npm run lint:write
    - npm run build

Deploy:
  image: ruby:latest
  stage: Build
  before_script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
  script:
    - ls
    - cd public/
    - dpl --provider=heroku --app=$HEROKU_APP_PRODUCTION --api-key=$HEROKU_API_KEY
