@import 'colors.pcss';
@import 'variables.pcss';

.content {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin-top: 45px;
  text-align: center;

  .title {
    font-weight: 600;
    font-size: 45px;
  }

  .description {
    padding-top: 5px;
    font-weight: 200;
    font-size: 25px;
  }
}
